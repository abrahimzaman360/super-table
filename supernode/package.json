{
  "name": "@supertable/supertable",
  "version": "0.1.1",
  "description": "High-performance JavaScript bindings for SuperTable",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "supertable",
    "triple": {
      "defaults": true
    }
  },
  "license": "MIT OR Apache-2.0",
  "keywords": [
    "table-format",
    "iceberg",
    "arrow",
    "parquet",
    "rust",
    "napi-rs"
  ],
  "engines": {
    "node": ">= 10"
  },
  "scripts": {
    "artifacts": "napi artifacts",
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi prepublish -t npm",
    "test": "node index.js",
    "version": "napi version"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}